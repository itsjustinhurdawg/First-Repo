var randomNumber = ["10","20", "30", "40",];
var number = Math.floor(Math.random() * randomNumber.length); // you use randomNumber.length because you might not know the length of the arrar
window.alert(randomNumber[number]);
